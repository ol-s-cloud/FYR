import {products} from "@/lib/products";
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=products.find(x=>x.slug===slug);if(!p)return <main className="pdp"><h1>PRODUCT NOT FOUND</h1></main>;return <main className="pdp"><section className="pdpGallery"><div className="pdpArt"><span>{p.category} / DROP 001</span><div className="pdpFigure"></div></div></section><section className="pdpInfo"><span className="pdpEyebrow">FIND YOUR RELIGION®</span><h1>{p.name}</h1><div className="pdpPrice">£{p.price}</div><p>{p.note}. Selected for movement, ritual and deliberate daily use.</p><div className="pdpSizes"><span>SELECT SIZE</span><div><button>XS</button><button>S</button><button>M</button><button>L</button><button>XL</button></div></div><button className="add">ADD TO BAG — £{p.price}</button><div className="pdpNotes"><span>MEN / WOMEN / ALL</span><span>SHIPPING CALCULATED AT CHECKOUT</span><span>30 DAY RETURNS</span></div></section></main>}